import withClearCache from './init/clear-cache';
import reportWebVitals from './init/reportWebVitals';
import API from './api';
import routerLinks from '../router-links';
import { keyRole, keyToken, keyUser, linkApi, keyRefreshToken, listStyle, language, languages } from './variable';

import convertFormValue from './convertFormValue';
import cleanObjectKeyNull from './cleanObjectKeyNull';
import covertChild from './covertChild';
import getSizePageByHeight from './getSizePageByHeight';
import loopMapSelect from './loopMapSelect';

export {
  withClearCache,
  reportWebVitals,
  routerLinks,
  keyRole,
  keyToken,
  keyUser,
  linkApi,
  keyRefreshToken,
  listStyle,
  language,
  languages,
  convertFormValue,
  cleanObjectKeyNull,
  covertChild,
  API,
  getSizePageByHeight,
  loopMapSelect,
};
